const path = require('path');

module.exports = {
  // 'config': path.resolve('config', 'database.js'), // config folder removed
  'models-path': path.resolve('models'),
  // 'seeders-path': path.resolve('seeders'), // seeders folder does not exist
  'migrations-path': path.resolve('migrations')
}; 